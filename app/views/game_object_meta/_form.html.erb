<%= form_for([game, game_object_metum]) do |f| %>
  <% if game_object_metum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_object_metum.errors.count, "error") %> prohibited this game_object_metum from being saved:</h2>

      <ul>
      <% game_object_metum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :sub_type %>
    <%= f.text_field :sub_type %>
  </div>

  <div class="field">
    <%= f.label :front_img %>
    <%= f.file_field :front_img %>
  </div>

  <div class="field">
    <%= f.label :back_img %>
    <%= f.file_field :back_img%>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
